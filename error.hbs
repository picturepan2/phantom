<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <title>{{meta_title}}</title>
  <meta name="HandheldFriendly" content="True" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" type="text/css" href="{{asset "css/spectre.min.css"}}" />
  <link rel="stylesheet" type="text/css" href="{{asset "css/phantom.min.css"}}" />
</head>
<body class="error-template">
  <div class="phantom">
    {{> "header"}}
    
    <main class="phantom-body" role="main">
      <div class="container">
        <div class="empty">
          <h1 class="empty-title">{{code}}</h1>
          <p class="empty-subtitle">{{message}}</p>
          <div class="empty-action">
            <a href="{{@blog.url}}" class="btn btn-primary">Go to the front page →</a>
          </div>
        </div>
      </div>
      {{#get "posts" limit="4"}}
      <div class="container">
        <div class="columns">
          {{#foreach posts}}
            <div class="column col-3 col-lg-4 col-md-6 col-sm-12">
              {{> "post-card"}}
            </div>
          {{/foreach}}
        </div>
      </div>
      {{/get}}
    </main>
  </div>
</body>
</html>